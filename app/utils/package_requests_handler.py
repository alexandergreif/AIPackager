def delete_package_request(packageID):
    #delete a package request from the database.
    # delete all the generated content, scripts and folder also.
    pass

def create_package_request(app_name, version, installer_type):
    #create a package_request with the app name, the version and the installer type,
    # also add other informations if available later on. Generate automatically the latest packageID
    pass

def update_package_request(packageID):
    # via the package ID, change information or the status of the package request
    #
    pass

def get_package_request(packageID):
    #read all the details of the package request.
    # Maybe show it to the user? Or store it in a variable and return it.
    # After that you can decide what to do with it.
    pass

